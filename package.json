{
  "name": "chrome_extension_test",
  "version": "1.0.0",
  "description": "This is a README.md file for a Chrome extension that adds the reading time to Chrome Extension documentation articles.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chrome-types": "^0.1.246"
  }
}
